import {useState, useEffect} from 'react';

const Home = () => {
    const [books, setBooks] = useState<

    return (
        <div>
            <h1>Home</h1>
        </div>
    );
};

export default Home;
